<div class="row mt-5">
    <div class="col-md-6 mx-auto card">
        <div class="card-header">
            <h1>Photos</h1>
        </div>
        <div class="card-body">
            <div class="card p-2 mt-2">
                <img src="{{home.firstUrl}}">
                <hr>
                <form method="POST" action="/picture1" enctype="multipart/form-data">
                    <input type="hidden" value="{{home._id}}" name="_id">
                    <input type="file" required name="image">
                    <button type="submit">Change</button>
                </form>
            </div>
            <div class="card p-2 mt-2">
                <img src="{{home.secondUrl}}">
                <hr>
                <form method="POST" action="/picture2" enctype="multipart/form-data">
                    <input type="hidden" value="{{home._id}}" name="_id">
                    <input type="file" required name="image">
                    <button type="submit">Change</button>
                </form>
            </div>
            <div class="card p-2 mt-2">
                <img src="{{home.thirdUrl}}">
                <hr>
                <form method="POST" action="/picture3" enctype="multipart/form-data">
                    <input type="hidden" value="{{home._id}}" name="_id">
                    <input type="file" required name="image">
                    <button type="submit">Change</button>
                </form>
            </div>
        </div>
    </div>
</div>